package danik.may.org.constant;

import java.awt.*;

public class Settings {

    private Settings() {
    }

    public static final int SCREEN_HEIGHT = Toolkit.getDefaultToolkit().getScreenSize().height -70;
    public static final double SCREEN_WIDTH = Toolkit.getDefaultToolkit().getScreenSize().width ;;
}
